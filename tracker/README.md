# OKX × Dubai Cares — Outreach Tracker

## Setup (5 minutes)

### Step 1: Create Supabase project
1. Go to https://supabase.com
2. Sign in / Sign up (free)
3. New Project ? name: okx-tracker
4. Wait for project to initialize (~1 min)

### Step 2: Run the SQL
In the Supabase dashboard ? SQL Editor:
1. Paste contents of setup.sql ? Run
2. Paste contents of seed.sql ? Run (loads all 73 creators)

### Step 3: Get credentials
Project Settings ? API:
- Copy Project URL
- Copy non public key

### Step 4: Configure tracker
Give the URL and anon key to DR Gero — the tracker.html gets updated in 2 minutes.

OR manually: open tracker/index.html, replace:
- REPLACE_WITH_SUPABASE_URL
- REPLACE_WITH_SUPABASE_ANON_KEY

### Live URL (after credentials configured)
https://cabdelkhalegh.github.io/OKX/tracker/
